#ifndef playerTurn_h
#define playerTurn_h

/**
 * Takes player turns, diceSum, dice total
 * and changes the turns
 * 
 */

void turn(int *turnPlayer1, int *turnPlayer2, int *turnPlayer3, int *turnPlayer4, int *turnPlayerCounter);
    
#endif